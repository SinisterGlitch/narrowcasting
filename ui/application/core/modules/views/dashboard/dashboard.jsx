var React = require('react');
var ReactRouter = require('react-router');

module.exports = React.createClass({
    render: function(){
        return (
            <div>
                test
            </div>
        );
    }
});