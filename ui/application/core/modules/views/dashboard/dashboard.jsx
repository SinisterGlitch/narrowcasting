var React = require('react');
var ReactRouter = require('react-router');

var Header = require('core/components/layout/app');

module.exports = React.createClass({
    render: function(){
        return (
            <div>
                test
            </div>
        );
    }
});