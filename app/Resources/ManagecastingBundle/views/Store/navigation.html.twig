<ul class="nav nav-pills">
    <li role="presentation" class="active">
        <a href="{{ path('manage_casting_store_index') }}">
            <span class="glyphicon glyphicon-list" aria-hidden="true"></span>
            Overview
        </a>
    </li>
    <li role="presentation" class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-expanded="false">
            Actions <span class="caret"></span>
        </a>
        <ul class="dropdown-menu" role="menu">
            <li>
                <a href="{{ path('manage_casting_store_new') }}">
                    <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span>
                    New store
                </a>
            </li>
            {% if app.request.attributes.get('_route') == 'manage_casting_store_detail' %}
                <li>
                    <a href="{{ path('manage_casting_store_edit', {'id': store.id}) }}">
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                        Edit store
                    </a>
                </li>
                <li>
                    <a href="{{ path('manage_casting_store_delete', {'id': store.id}) }}">
                        <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                        Delete store
                    </a>
                </li>
            {% endif %}
        </ul>
    </li>
</ul>